import styled from "styled-components";
import {
  fontFamily,
  textAlign,
  space,
  fontWeight,
  fontSize
} from "styled-system";

export const Text = styled.div`
 ${fontFamily}
  ${textAlign}
  ${space}
  ${fontWeight}
  ${fontSize}
`;
