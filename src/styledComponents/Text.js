import styled from "styled-components";
import {

  textAlign,
  space,
  fontWeight,
  fontSize
} from "styled-system";

export const Text = styled.div`
 font-family: 'Open Sans', sans-serif;
  ${textAlign}
  ${space}
  ${fontWeight}
  ${fontSize}
`;
